# This should be merged into ../Makefile.am
# However, first we need to make sure gnu/java-classes.stamp
# is built at the right time, so more cleanup is needed first.

all:
	cd .. && $(MAKE) gnu-classes.stamp
